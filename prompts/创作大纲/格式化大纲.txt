// 双斜杠开头是注释，不会输入到大模型
// 文件开头结尾的空行会被忽略

// 输入：y
// y：即要格式化的大纲，可以是整个大纲，也可以是其中的一部分

user:
你需要将**大纲**转化为正确的小说大纲格式，遵循以下规则：

1. 每段描述一个关键事件，从宏观角度去梗概主角和其他人物在某地方发生了什么，导致什么后果，有什么收获
2. 每段不能有标题，不能有序号。
3. 不要进行环境、外貌、语言、心理描写，也不要描述具体行为。关注大的事件。

4. 如果输入的已经是大纲格式，保持原样输出。
5. 如果输入的不是大纲格式，请将其转化为上述大纲格式。

请对**大纲**进行处理，在三引号(```)文本块中输出符合要求的大纲格式。